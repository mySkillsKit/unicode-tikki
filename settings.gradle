pluginManagement {
    repositories {
        maven { url 'https://cache-redirector.jetbrains.com/packages.jetbrains.team/maven/p/ij/intellij-dependencies' }
        maven { url 'https://cache-redirector.jetbrains.com/intellij-repository/releases' }
        maven { url 'https://packages.jetbrains.team/maven/p/ij/intellij-dependencies' }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'unicode-tikki'

